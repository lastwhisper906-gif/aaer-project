# Daily Log — 2026-07-05 (토) / 인프라·가드 강화 세션

## 계기

사용자 요청으로 채점 보조 Claude가 저장소 전체 감사를 수행, 발견 사항을 코드로 해결.
판단 의존 결정 4건은 각각 추천안 + 대안 2개(근거 포함) 제시 후 **사용자가 개별 선택·서명**
(2026-07-03 v1의 "일괄 승인" 반성의 실천 — 이번엔 선택지 비교 후 확정).

## 중대 발견: `~/aaer-data/` 소실 → 복원

금요일 저녁 재검증의 원문 사본(`~/aaer-data/{ticker}/`)이 **디스크에 존재하지 않았다**
(재검증 diff·REVIEW.md가 참조하는 경로). `tools/fetch_primary_sources.py` 재실행으로
30건 전수 복원 (24MB, 실패 3건은 SEC 페이지 자체의 자리표시자 링크 `compxxxxx.pdf` — 실문서 아님).
**교훈**: git 밖 데이터는 소실이 조용히 일어난다 — 월요일 검토 전 존재 확인이 체크리스트에 있어야 했다.

## 서명된 결정 4건 (모두 추천안 채택)

1. **피평가자 입력 분리 = 사전 생성 파일 방식**: `tools/build_evaluatee_inputs.py` →
   `data/evaluatee/cases.json` (화이트리스트: case_id/ticker/cik/company_name/cutoff_date —
   group도 정답의 일부라 제외). `schemas/evaluatee_input.json`(additionalProperties: false) +
   3중 방어 테스트(재생성 대조·스키마·금지 필드 목록).
2. **기간 정밀도 = 패턴 완화**: `manipulation_period_*`를 `^\d{4}(-\d{2})?(-\d{2})?$`로 —
   데이터 무변경, 검증기 허용목록(편차 48건) 삭제, "지어내지 않는다" 원칙의 스키마 반영.
3. **scheme_type = if/then 그룹 분기**: treatment 필수 / control 필드 금지 (v1 모순 #1 해소,
   "required만 강제력" 교훈의 반영). matched_case_id도 동형 분기(control 필수 / treatment 금지).
4. **스키마 번들 지금 적용**: matched_case_id 추가(#4) + llm_output.documents_used
   required·minItems 1 승격(#2). → case_input v1.1, llm_output v1.1.

## 코드 산출물 (전부 결정론·테스트 포함, 15 passed)

- **cutoff_guard v1**: ① 중복 case_id = fail-closed 예외(구현은 조용한 last-wins였음),
  ② `accession_no` 역조회 — 호출자 신고 doc_date를 로컬 EDGAR submissions JSON의 filingDate와
  가드가 직접 대조, 불일치·미보유 fail-closed. 호출자 메타데이터 신뢰 문제 해소.
- **가드 우회 정적 스캔** (`pipeline/test_no_guard_bypass.py`): pipeline/ 내 네트워크 직접
  import + candidates.json 직독 금지를 테스트로 강제 (규범 → 기계 강제).
- **재현성**: requirements.txt(고정 버전), `tools/validate_schemas.py`(스키마·데이터 자동 검증),
  GitHub Actions CI(pytest + 검증기). 이 세션에서 pytest 미설치로 테스트 실행 불가였던 상태 해소.
- **위생**: .gitignore(.DS_Store/.venv), `scoring/overrides.md` 스켈레톤(OV-NNN 형식),
  COLLECTION_NOTES §5에 "재검증 이전 스냅샷" 주의 문구.

## A/B 기준 초안에 설계 선택 #4 추가 (판정 아님 — 월요일 안건)

G1의 '합산 6개' 집계 문제: 연차 6개와 분기 6개는 Beneish 변수 산출 가능성이 다름 —
개수와 함께 구성(예: 연차 ≥2 + 총 ≥6) 확정 필요. T30 LK(연차 0개)가 직접 걸림.
REVIEW.md 항목 3에 연동 반영.

## 보류 (사용자 확정 대기)

- **main 머지 여부**: 추천 = 지금 --no-ff 머지+push (사전 고정 문서가 기본 브랜치에 있어야
  증거력, 커밋 타임스탬프 보존). 대안: PR 검토 후 머지 / 월요일 서명 후 머지. 무응답으로 보류.
- **금요일 회고란(2026-07-03.md) 미기입** — §8-7의 두 재검토 창구 중 하나가 빈 채로 지남.
  소급 기입 권장.

## 방법론 문구 (§5-5)

본 세션의 감사·정정은 Claude 기반 단일 파이프라인의 저장소에 한정되며, 판단 의존 결정의
최종 확정은 전건 인간 서명으로 수행되었다 (채점: Claude 보조 + 인간 최종 확정).

---

# 저녁 세션 (동일 07-05) — 머지·매니페스트·게이트 패키지

## main 머지 (오전 보류분 — 사용자 서명으로 확정, 지시문 §0)

- push 복구: 로컬 5커밋은 세션 시작 전 push 완료 상태 확인 (`git fetch` 후 ahead 0).
- `--no-ff` 머지 `3ca0794` (16커밋, 사유는 커밋 메시지에 기록) → push.
- **패리티 검증 기록**: `git log --oneline -3 main` == `origin/main` (헤드 `3ca0794` 동일),
  `git rev-list --count main...origin/main` = **0**. 검증 시각 커밋 직후.

## 무결성 매니페스트 (오전 소실 사고의 재발 대비)

- `data/manifests/aaer_data_manifest.json`: 153파일, 24,664,847 bytes, 파일별
  SHA-256·size·fetched_at(mtime)·source_url(153건 전건 역산 성공, 파생 .txt는 derived_from).
- `tools/verify_manifest.py`: --write 생성 / 기본 전수 대조(fail-closed — 디렉터리 부재도
  실패) / --schema-only(CI). URL 로직은 fetch_primary_sources.py import로 재사용
  (신규 파일 2개 — §8-3 준수). 변조 테스트: 1바이트 추가 → exit 1 → 원복 → PASS 재확인.
- CI에 --schema-only 단계 추가 (러너엔 원본 없음).

## 실패 fetch 3건 전수 추적 → GP-7 결함 대장 (판정·해결 없음)

지시문 전제("케이스 3건")가 실측과 달랐다: **실패 URL 3 / 케이스 2 / 실공백 1**.
- T14 PWE ×2: SEC 페이지 자리표시자 링크 — 동일 소장을 comp24809.pdf로 보유, 공백 아님.
- T19 OSIR ×1: **진짜 공백** — 금요 재검증이 ¶29 축어 인용한 comp-pr2017-207.pdf가
  복원본에 없음 (즉 07-05 복원은 완전 복원이 아니었다 — 매니페스트 기준선은 이 결함
  상태의 스냅샷임을 GP-7에 명기).
- T30 LK: 기존 손상 건으로 통합 기재.

## GATE_PACKAGE.md 신설 (월요일 서명용 결정 시트)

GP-0(선행 재서명) / GP-1(WAGE 수치 관용) / GP-2(폭로일 원칙+12건) / GP-3(MRVL·VRX 기간
원칙) / GP-4(무AAER 편입) / GP-5(A/B 기준, 설계선택 4 — T14 PWE의 40-F 연차가 G1 구성
문언에 걸림을 추가 적시) / GP-6(킬 스위치 절차·자료) / GP-7(데이터 결함 ①②③+영향 표).
전 항목 근거 pinpoint + 서명란 + 학습 노트. **이 세션의 판정·서명 0건.**

## 학습 노트 (§10)

"없는 3건"조차 검증 대상이었다 — 죽은 링크(T14)·진짜 공백(T19)·읽히는 손상(T30)은 서로
다른 결정을 요구하며, 결함의 *분류*가 결함의 *개수*보다 결정에 가깝다.

## 방법론 문구 (§5-5)

본 저녁 세션의 산출물은 Claude 기반 단일 파이프라인의 저장소·수집물에 한정되며, 게이트
항목의 최종 확정은 전건 월요일 인간 서명 사항이다 (채점: Claude 보조 + 인간 최종 확정).

---

# 심야 세션 (동일 07-05) — 표본 점검 후속: 값 수준 누출 2건 정정

## 발견의 성격 (오버라이드 첫 2건 — §9 지표의 첫 데이터 포인트)

사용자가 `data/evaluatee/cases.json`을 §7 표본 점검 → 기존 3중 방어가 **구조적으로
잡을 수 없는** 누출 2건 발견 (3중 방어는 전부 '어떤 필드가 존재하는가'만 검사, 필드
'값'은 안 봄). 채점 보조 Claude가 실측 검증으로 실재 확인 후 서명 결정대로 정정.
`scoring/overrides.md` OV-001(case_id T/C 접두사 = 그룹 인코딩)·OV-002(후신 사명 =
값 안의 §5-1 위반, 최악 T18 Bausch — 개명이 스캔들의 결과물) 기록.

## 정정 내용 (commit `d63c9d8` — §5-6 최후 변경 창, 사유는 커밋 메시지·REVIEW.md에)

- 빌더 수정(출력 수기 편집 아님): 중립 ID(case_NN) + 고정 시드 20260705 셔플, 후신
  사명 제거(f/k/a는 유지 — T08 확인), 복합 티커 정규화(T29 UAA/UA→UAA, 30건 중 유일).
- 매핑 `data/scoring/id_mapping.json` 분리 (피평가자 경로 밖, pipeline/ 참조 금지 스캔 추가).
- 스키마 evaluatee_input v1.1 (`^case_[0-9]{2}$`, 티커 단일값 패턴).
- **방어 ④ 신설(값 수준 스캔)**: 중립 ID 형식·금지 부분열(n/k/a·aaer·fraud 등 20종)·
  매핑 전단사·셔플 실효성·경로 분리. 변조 주입 테스트로 fail-closed 확인 (6 failed →
  원복 → 20 passed).

## 실측이 정정한 지시문 전제 3건

1. §2-1의 세 날짜(T25 07-20 / T07 06-28 / T18 10-18)는 폭로일이 아니라 **컷오프일**
   (폭로일-1) — 사건·소스 인용과 함께 GP-2 보강으로 기입.
2. **대조군은 아직 없다** (matched_case_id 0건) — "매칭 감사"는 불가능, 대신 매칭 수행
   전 기준 서명 안건으로 전환 (GP-8, §5-6상 오히려 적기).
3. 대조군 컷오프 규약은 이미 서명돼 있음 (스키마 v1 "매칭 실험군 복사", `a333ee4`) —
   신규 결정이 아니라 쌍 날짜 연결이라는 약한 누출의 재검토 안건 (GP-9).

## 신설 문서

`docs/methodology_limitations.md` — L-1 암기 한계 (중국 RTO 군집 6건, cutoff_guard는
문서 날짜만 통제·모델 내부 지식은 통제 불가, 중립 ID도 회사 식별을 못 막음) +
memorization_suspect 채점 플래그 초안 (플래그 포함/제외 두 벌 병기).

## 학습 노트 (§10)

방어가 필드 목록을 볼 때 누출은 값 안으로 이동한다 — 그리고 look-ahead의 세 번째
채널(모델 내부 지식)은 차단이 아니라 측정·공개의 대상이다.

## 사용자 검토 회신 반영 (심야 2차 — 정정 2건 승인 + 조건 4건)

정정 2건·전제 정정 3건 모두 승인. 조건·지시 반영:
- 매핑 `data/scoring/` → **`scoring/`** 이동 (성격: 데이터가 아니라 채점 인프라 —
  scoring/은 이미 '피평가자가 볼 수 없는 인간 전용 구역' 의미론). 시드 평문은 유지 승인
  (위협 모델 = 컨텍스트 오염이지 암호 공격자가 아님).
- **페이로드 규약 명문화** (GP-6): 방어 ④는 파일 위치만 지킨다 — 최종 보증은 "피평가자
  호출 페이로드 = evaluatee_input v1.1 필드만 + 저장소 컨텍스트 주입 금지 + 케이스별
  독립 호출". 채점 프로토콜 고정 시 서명.
- GP-9 잠정 기본값 ①에 성립 조건 추가: 독립 호출일 때만 충분, 배치 호출이면 실누출 →
  재검토.
- GP-2 T18: 원칙 문언 먼저 서명 → SIRF 채택이 기계적 귀결인지 판별 (역순 금지).
- L-1 플래그 기준 기계 고정: 명부(중국 RTO 6건) AND 기계적 인용 결손, 두 벌 병기 유지.

## 방법론 문구 (§5-5)

본 심야 세션의 산출물은 Claude 기반 단일 파이프라인의 저장소에 한정되며, 누출 정정은
사용자 서명 결정의 집행, 게이트 신규 항목(GP-8/GP-9)의 확정은 월요일 인간 서명 사항이다
(채점: Claude 보조 + 인간 최종 확정).

---

# 심야 세션 2차 (동일 07-05) — 유도 서명 세션: 게이트 전 항목 확정

## 경위 (불변 조항 3의 작동 기록)

사용자 "서명 그냥 지금 다해버릴게" → 채점 보조가 **일괄 승인 = 인간 서명의 형해화**임을
지적(v1 전례 `a333ee4` 인용, GP-6은 서명 대상이 아니라 절차임도 지적) → 사용자가 **유도
서명 세션** 선택. 항목당 결정 질문 + 추천안 + 반대근거 제시, 사용자가 개별 선택.
**이 라운드 오버라이드 0건(전 항목 추천안 채택)** — §9 기준 고무도장과 구별되는 근거:
각 항목이 개별 질문으로 제시되었고 선택지·반대근거·기계적 귀결(GP-2 preview)이 첨부됨.
다만 "전 항목 추천 채택" 패턴 자체는 기록해 둔다 — Week 3 사후 확인 시 참조.

## 서명 내역 (GATE_PACKAGE.md 서명란에 전문)

- GP-0: 스키마 4파일 + 재검증 diff 전건 승인 / GP-1: ① 명령문 수치 기준
- **GP-2 원칙 A**: "최초의 공개적·회계특정적 신호(소스 불문, 비공개 내부인지 제외)" →
  기계 적용으로 **폭로일 5건 이동** (T02→2010-08-11 [10-Q EDGAR 실측], T12→2013-08-07
  [10-K/A], T13→2014-05-13 [명령 ¶33, EDGAR vehicle 미확인 주석], T19→2015-11-06 [8-K],
  T26→2016-12-15 [내부고발 소송 보도]) + T18은 특정성 판단으로 10-19 SIRF 유지.
  컷오프 연동 이동, 분기수 결정론 재계산 (T02 66, T12 21, T13 29, T19 37, T26 35).
- GP-3: 명령문 정의 창 (T17 Relevant Period ¶2 / T18 2014-07 유지)
- GP-4: ② 집행조치 기준(30건 유지) + T16 동일 사건 기준 → **AAER-4105 기입** (무AAER 4건 확정)
- GP-5: **A/B 기준 v1 고정** — D1 좁은 문언 / 폭로 경로 배제 / 임계 6 / G1 = 연차≥2 AND
  총≥6, 40-F 포함 (T30 G1 no 방향·T14 G1 yes 인지 서명). ab_criteria_draft.md v1 SIGNED.
- GP-7: ① 유지+주의문 / GP-8: ① 장르 내 매칭 / GP-9: ① 유지+독립호출 연동
- 실행 규약 3조항 (페이로드·독립 호출·memorization_suspect 명부 규칙)

## 잔여

GP-6 킬 스위치: 1차 표(30케이스 × G/F/T/D + 근거 인용) 지금 작성 지시받음 — 작성 후
A형 집계·GO/STOP은 본인 확정. T13 EDGAR vehicle(05-13 당일 제출물) 후속 확인 항목.

## 학습 노트 (§10)

"다 서명"과 "다 검토하고 서명"의 차이는 소요 시간이 아니라 **기록의 증거력**이다 —
같은 결론이라도 선택지·반대근거를 보고 고른 기록만이 오버라이드 지표(§9)의 분모가 된다.
